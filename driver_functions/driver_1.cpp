#// funtion to print sum of two numbers

#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> t;

        int arr[n];
        for (int i = 0; i < n; i++)
            cin >> arr[i];
        //  Solution ob;
        //  cout<< ob.firstRepeated(arr,n)<<endl;
    }
    return 0;
}
